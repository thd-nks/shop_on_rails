<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
</head>
<body>
<table>
  <tr>
    <td>Name</td>
    <td>Price</td>
    <td>Amount</td>
  </tr>
  <% @items.each do |key, value| %>
    <tr>
      <td><%= value[:name] %></td>
      <td><%= value[:price] %></td>
      <td><%= value[:amount] %></td>
      <td><%= link_to 'Delete', item_path(key), method: :delete %></td>
    </tr>
  <% end %>
</table>
<%= link_to 'Back', items_path %>